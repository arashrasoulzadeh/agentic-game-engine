name: engine_flutter
description: Flutter rendering/input/camera shell for engine_core. No gameplay logic lives here.
version: 0.1.0
publish_to: none

environment:
  sdk: ^3.9.0
  flutter: ">=3.0.0"

dependencies:
  flutter:
    sdk: flutter
  engine_core:
    path: ../engine_core

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
