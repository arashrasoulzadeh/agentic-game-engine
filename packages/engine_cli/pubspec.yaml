name: engine_cli
description: CLI for scaffolding and updating Flutter games built on engine_core.
version: 0.1.0
publish_to: none

environment:
  sdk: ^3.9.0

dependencies:
  args: ^2.5.0
  path: ^1.9.0

dev_dependencies:
  test: ^1.25.0
  lints: ^5.0.0

executables:
  game_agent: game_agent
